import React from "react";

const Home = () => {
  return (
    <div>
      <button>Doctor</button>
      <button>User</button>
      <button>Appointment</button>
    </div>
  );
};

export default Home;
